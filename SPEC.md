# SPEC — 경기대학교 재학생 친구 만들기 앱

문서 버전: 초안 1 / 작성일: 2026-08-19
용도: 공모전·해커톤 제출용 기획서

이 문서는 기획 단계 산출물이다. 이번 작업의 범위는 **본 문서까지**이며, 구현 착수 여부와 형태는 별도로 결정한다.
확정된 항목만 단정형으로 적었고, 정해지지 않은 항목은 추측하지 않고 `[확인 필요]`로 남겼다.

**선행 프로젝트와의 관계**: 같은 저자의 소개팅 앱 기획(`../소개팅 어플/SPEC.md`, 경기랑)의 **방향 전환 후속작**이다. 사용자 경계와 이메일 인증 방식은 물려받되, 목적을 연애에서 친구로 바꾸면서 매칭 방식·프로필 공개 범위·안전장치를 전부 다시 설계했다. 무엇이 왜 바뀌었는지는 부록 B에 정리했다.

---

## 1. 목표

경기대학교 안에서 **"같이 뭔가 할 사람"을 찾을 경로**를 만든다.

연애 상대를 찾아주는 앱이 아니다. 밥을 같이 먹을 사람, 운동 메이트, 공모전 팀원, 취미가 같은 사람 — 목적을 가리지 않고 **함께할 사람**을 찾는 것이 목표다.

---

## 2. 사용자

- **대상**: 경기대학교 소속 전체 — 학부생, 대학원생, 휴학생 (수원·서울 캠퍼스 구분 없음)
- **제외**: 교직원, 졸업생, 타 대학 학생
- **연령 정책**: 만 19~30세를 대상으로 안내한다. 다만 이는 **자격 판정 조건이 아니다** (4.1 참고)

**휴학생을 포함한 이유**: 휴학 중일 때야말로 학교 안 인간관계가 끊기기 쉽고, 이 앱이 필요한 시점이다. 대학원생을 포함한 이유는 학부와 대학원 사이에 교류 경로가 거의 없기 때문이다.

작은 사용자 풀(수십~수백 명)이라는 점이 아래 설계 결정 대부분의 근거다.

---

## 3. 해결하려는 문제

| 문제 | 이 앱의 대응 |
|---|---|
| 같이 밥 먹을 사람, 같이 운동할 사람을 구할 통로가 학과·동아리 밖에 없다 | 관심사 태그로 조건을 걸어 학교 전체에서 검색 |
| 에브리타임 같은 익명 커뮤니티는 글은 오가지만 **사람으로 이어지지 않는다** | 검색 → 신청 → 수락 → 1:1 채팅이라는 명확한 경로 |
| 일반 친구 앱·오픈채팅에는 학생이 아닌 사람과 업자가 섞여 있다 | 학교 이메일 인증으로 소속 확인 |
| 처음 만나면 무슨 말을 해야 할지 모른다 | 만난 자리에서 함께 보는 밸런스게임 화면 (4.6) |

**경쟁 서비스와의 차별점**

- **에브리타임**: 익명 게시판. 정보는 얻지만 특정 개인과 연결되는 경로가 없다. 이 앱은 처음부터 사람을 찾는 것이 목적이다.
- **소개팅 앱**: 목적이 연애로 고정되어 있고 외모 중심이다. 이 앱은 사진을 선택 항목으로 두고 관심사 태그를 전면에 세워, 연애 앱과 다른 판단 기준을 만든다.
- **오픈채팅**: 상대의 소속을 확인할 수 없다. 이 앱은 학교 이메일 인증을 통과한 사람만 들어온다.

---

## 4. v1 기능

v1의 경계는 **"검색해서 만나고, 만난 자리에서 어색하지 않게 하는 것"**까지다. 커뮤니티는 v2로 미룬다 (5장).

### 4.1 재학생 인증

- `@kyonggi.ac.kr` 주소로 6자리 인증코드를 발송하고, 입력하면 가입이 완료된다
- 학생증 사진·신분증은 받지 않는다 (개인정보 최소 수집)
- 인증을 마치지 않으면 서비스의 어떤 화면도 이용할 수 없다

**연령 제한을 자격 요건으로 두지 않는 이유**

이메일 인증은 나이를 증명하지 못한다. 서버가 아는 것은 "이 사람이 학교 메일함에 접근할 수 있다"는 사실뿐이다. 생년월일을 입력받아 걸러도 그것은 자기신고일 뿐이라 실효가 없고, 실효를 확보하려면 휴대폰 본인인증을 붙여야 하는데 이는 수집하는 개인정보를 크게 늘린다.

그래서 **만 19~30세는 서비스 안내 문구와 이용약관에 적는 대상 연령**으로 두고, 실제 입장 자격은 학교 이메일 인증이 담당한다. 나이는 프로필 표시와 검색 필터에만 쓴다.

**학교 이메일의 한계 (알려진 위험)**: 졸업 후에도 계정이 유지되는 경우가 있어, 이메일 인증만으로는 "지금 재학 중"을 보장하지 못한다. v1은 이 위험을 감수하고, 문제가 발생하면 신고를 통해 처리한다. 재인증 주기 도입 여부: `[확인 필요]`

### 4.2 프로필

**관심사 태그가 프로필의 주인공이다.** 화면에서 가장 크게 보이는 것은 얼굴이 아니라 태그다.

| 항목 | 필수 | 공개 |
|---|---|---|
| 닉네임 | 필수 | 공개 |
| 나이 | 필수 | 공개 |
| 학과 | 필수 | 공개 |
| 관심사 태그 (최대 5개) | 필수 | 공개 |
| 한 줄 소개 | 필수 | 공개 |
| 프로필 사진 | **선택** | 공개 |
| 성별 | 필수 | 공개 |
| 실명 · 학번 · 이메일 주소 | 필수 입력 없음 | **비공개** |

- **사진을 선택으로 둔 이유**: 사진을 필수로 하면 가입 문턱이 올라가고, 무엇보다 사람을 외모로 거르는 소개팅 앱의 판단 방식을 그대로 들여오게 된다. 사진이 없으면 관심사 태그를 배경으로 한 기본 이미지를 보여준다.
- **관심사 태그는 고정 목록에서 고른다.** 자유 입력을 허용하면 같은 취미가 표기 차이(운동/헬스/웨이트)로 갈라져 검색이 깨지고, 부적절한 문구가 섞일 여지가 생긴다. 태그 최종 목록: `[확인 필요]` — 제안 예시: 운동·헬스, 러닝, 등산, 게임, 영화, 음악, 독서, 카페, 맛집, 여행, 사진, 그림, 요리, 반려동물, 공모전·대외활동, 어학·교환학생, 자격증·스터디, 창업, 코딩, 보드게임
- **학과를 그대로 공개하고, 같은 학과 사람도 검색된다.** 소개팅 앱이었다면 아는 사람과 마주치는 부담 때문에 계열 수준으로만 표기했겠지만, 친구를 찾는 앱에서는 같은 학과 사람을 찾고 싶은 수요가 실제로 존재한다 (같은 수업, 같은 건물).

**익명성이 없다는 점을 전제로 삼는다.** 학과 + 나이 + 성별이면 소규모 학과에서는 사실상 신원이 특정된다. 이를 숨기려 어중간하게 가리는 대신, **누군지 드러나는 것을 전제로 안전장치(4.5)를 두껍게 설계했다.** 가입 화면에 이 사실을 명시한다.

### 4.3 검색과 필터

사용자가 **원할 때 원하는 조건으로** 사람을 찾는다. 시스템이 정해주는 추천은 없다.

**필터 항목**

1. 성별 (남 / 여 / 상관없음) — **기본값은 "상관없음"**
2. 나이대 범위
3. 학과 (특정 학과 지정 / 내 학과만 / 상관없음)
4. 관심사 태그 (여러 개 선택 시 하나라도 겹치면 노출)

**성별 필터의 성격**: 이 앱에서 성별 필터는 연애 상대를 고르는 장치가 아니라 **편안함과 안전감을 위한 선택지**다. 그래서 기본값을 "상관없음"으로 두고, 화면 문구도 "만나고 싶은 성별"이 아니라 "성별"로 적는다.

**검색 결과에서 제외되는 사람**: 나 자신, 내가 차단한 사람, 나를 차단한 사람, 정지 중인 사람.

**유사도 점수·추천 알고리즘은 v1에 넣지 않는다.** 후보가 수십~수백 명인 상황에서는 알고리즘이 결과를 바꾸지 못한다. 사용자가 늘어난 뒤 개선 과제로 둔다.

정렬 기준(최근 접속순 / 태그 일치 수순 / 무작위): `[확인 필요]` — 제안: 최근 접속순. 활동 중인 사람이 위에 오는 편이 신청이 실제로 읽힐 확률을 높인다.

### 4.4 친구 신청 → 수락 → 1:1 채팅

```
검색 결과에서 프로필 열람 → 인사말 한 줄과 함께 친구 신청
   → 상대가 수락 → 1:1 대화방 생성
   → 상대가 거절 또는 무응답 → 아무 일도 일어나지 않음
```

- **수락 전에는 메시지를 보낼 수 없다.** 신청에 첨부한 인사말 한 줄이 수락 전에 전달할 수 있는 전부다.
- **인사말 한 줄은 필수다.** 빈 신청은 받는 쪽에서 판단할 근거가 없어 수락률이 낮고, 한 줄을 쓰게 하는 것만으로 무성의한 대량 신청이 크게 줄어든다.
- **하루 신청 상한: 5건** (제안값, `[확인 필요]`). 목록 탐색 방식에는 신청 횟수를 제한하는 자연적 장치가 없어, 상한이 없으면 한 사람이 전체 사용자에게 신청을 뿌릴 수 있다. 상한은 그 유일한 방어선이다.
- **거절은 상대에게 알리지 않는다.** 목록에서 조용히 사라지고, 거절당한 사람에게는 "응답 없음"과 구별되지 않게 보인다.
- 같은 사람에게 재신청 가능 여부와 대기 기간: `[확인 필요]` — 제안: 거절 후 30일간 재신청 불가

**채팅 범위 (v1)**: 텍스트 메시지 입력·전송·표시, 대화 내용 보관, 대화방 목록의 마지막 메시지 미리보기, 안 읽은 메시지 표시.

**채팅에서 v1에 넣지 않는 것**: 사진·파일·이모티콘 전송, 읽음 확인 표시, 메시지 삭제·수정.

카카오톡 아이디 교환 방식은 채택하지 않는다. 앱을 다시 열 이유가 사라지고, 문제가 생겼을 때 관리할 수단이 없어진다.

실시간 전송 방식(폴링/웹소켓 등), 대화 내용 보관 기간, 신고 시 보존 여부: `[확인 필요]`

### 4.5 차단 · 신고 · 관리자 검토

**차단과 신고는 완전히 다른 기능이다.** 이 구분이 이 장의 핵심이다.

| | 차단 | 신고 |
|---|---|---|
| 의미 | "이 사람을 그냥 안 보고 싶다" | "이 사람이 규칙을 어겼다" |
| 잘못을 전제하는가 | 아니다 | 그렇다 |
| 제재로 이어지는가 | **아니다** | 그렇다 |
| 상대가 아는가 | 모른다 | 모른다 |

**차단**

- 검색 결과, 게시판, 대화방 등 **모든 화면에서 양방향으로 사라진다.** 내가 상대를 차단하면 상대도 나를 볼 수 없다
- 상대에게 차단 사실을 알리지 않는다
- 언제든 해제할 수 있다
- **차단 횟수는 어떤 제재의 근거도 되지 않는다**

**차단 횟수로 자동 정지하지 않는 이유** (당초 구상에서 변경)

처음에는 "7명 이상에게 차단당하면 한 달 이용 금지"를 검토했다. 이를 채택하지 않는다.

차단은 잘못의 증거가 아니다. 말투가 안 맞아서, 관심사가 달라서, 그냥 취향이 아니라서 차단하는 것이 정상적인 사용이다. 차단 횟수를 제재 기준으로 삼으면 **아무 잘못도 하지 않았는데 인기가 없다는 이유로 한 달간 쫓겨나는 사람**이 생긴다. 게다가 이 앱은 조건 검색 방식이라 한 사람이 여러 사람에게 노출되는 빈도가 높아 차단이 빠르게 쌓인다. 무엇보다, 정지당한 사람에게 "당신은 잘못한 게 없지만 여러 명이 당신을 숨겼습니다"라고 설명할 방법이 없다.

**신고**

- 프로필·대화방·게시글에서 상대를 신고할 수 있다
- 신고하면 두 사람은 자동으로 상호 차단 상태가 된다
- **서로 다른 사용자 3명 이상이 같은 사람을 신고하면 자동으로 "검토 대기" 상태**가 되어 관리자 목록 상단에 올라간다 (임계치는 제안값, `[확인 필요]`)
- **검토 대기 중에도 노출은 유지된다.** 관리자가 판단하기 전까지는 정지하지 않는다. 담합 신고로 정상 사용자를 쫓아내는 것을 막기 위한 조치다
- **정지 여부는 사람이 판단한다.** 정지 기간의 기본값은 30일로 하되 관리자가 조정할 수 있다
- 신고 사유 카테고리: `[확인 필요]` — 제안: 욕설·비하, 성적 불쾌감, 사기·홍보, 만남 강요, 프로필 사칭, 기타

**관리자 화면 (v1 포함)**

- 신고 목록을 최신순·누적 신고 수순으로 보고, 신고 내용과 신고 대상 프로필을 확인한다
- 각 신고에 대해 `기각` / `경고` / `정지(기간 지정)` 중 하나를 선택한다
- 정지된 사용자는 로그인 시 정지 사유와 해제 예정일을 본다
- **운영 주체: 팀원 1명이 관리자 역할을 맡는다.** 담당자 지정: `[확인 필요]`
- 이의제기 절차: v1에는 넣지 않는다. 정지된 사용자가 관리자에게 연락할 수단(이메일 주소 안내)만 화면에 표기한다

### 4.6 밸런스게임 — 대면 아이스브레이킹

**이 앱만의 기능이다.** 다른 친구·매칭 서비스는 앱 안에서 대화가 이어지게 하는 데까지만 관여하고, 실제로 만난 자리는 사용자에게 맡긴다. 그런데 처음 만난 자리의 어색함이야말로 관계가 한 번으로 끝나는 가장 큰 이유다.

- 대화방 안에서 **"만났어요"** 버튼을 누르면 대면 모드가 열린다
- 화면에는 밸런스게임 질문 하나가 **큰 글씨로** 표시되고, 두 사람이 폰 하나를 가운데 두고 함께 본다
- 넘기기 버튼으로 다음 질문을 본다
- **답을 입력하지도, 저장하지도 않는다.** 말로 하는 것이 목적이므로 앱은 질문만 던지고 빠진다

**답을 저장하지 않는 이유**: 답을 기록하면 그 자체가 개인정보가 되고, "상대가 뭐라고 답했나"를 나중에 확인하는 기능이 필요해지며, 대화가 화면 조작으로 바뀐다. 저장하지 않으면 서버도 필요 없어 구현 비용이 거의 들지 않는다 — 질문 목록과 화면 하나가 전부다.

질문 목록: `[확인 필요]` — 30~50개 규모를 제안한다. 정치·종교·외모·연애 경험에 관한 질문은 넣지 않는다.

---

## 5. v2 이후

### 5.1 커뮤니티 — 관심사 태그별 게시판 (v2)

- 프로필에서 쓰는 **관심사 태그와 같은 목록**으로 게시판을 나눈다. "운동·헬스 게시판", "공모전·대외활동 게시판" 식이다
- 태그가 프로필·검색·게시판 세 곳에서 같은 축으로 쓰이므로 앱이 하나의 구조로 읽힌다
- 글에서 작성자 프로필로 이동해 바로 친구 신청을 할 수 있다 — 검색으로 못 찾는 사람을 만나는 두 번째 경로가 된다
- 실명이 아닌 프로필 닉네임으로 글을 쓴다 (완전 익명 아님). 익명 게시판은 관리 부담이 크고 에브리타임과 정면으로 겹친다

**v2로 미룬 이유**: 사용자가 적을 때 게시판을 열면 글이 안 올라와 텅 빈 화면이 되고, 빈 게시판은 앱 전체가 죽은 것처럼 보이게 만든다. 검색·신청 기능으로 사용자가 일정 수준 모인 뒤에 여는 것이 맞다. 개설 기준이 되는 사용자 수: `[확인 필요]`

### 5.2 유료 기능 (검토 대상, 시기 미정)

**사용자가 충분히 모이기 전까지 유료화하지 않는다.** 작은 풀에서 유료 기능을 도입하면 무료 사용자의 경험이 눈에 띄게 나빠져 남은 사람마저 떠난다.

**하지 않을 것 (명시적 배제)**

- **검색 결과 상단 노출 판매** — 좁은 학교 안에서는 "돈 낸 사람만 보이는 앱"이 되어 신뢰가 무너진다
- **"나를 본 사람 / 나에게 신청하려던 사람" 보기** — 사람을 상품처럼 다룬다는 인상을 준다

**검토할 방향**

- 하루 신청 상한 확대 (공정성을 비교적 지키면서 수익이 나오는 형태)
- 프로필 꾸미기 요소, 밸런스게임 질문 확장팩 등 **매칭 공정성과 무관한 것**

### 5.3 그 밖의 후보

- 3~4명 소그룹 매칭 (오프라인 모임과 밸런스게임에 더 적합하지만, 1:1이 자리 잡은 뒤에 검토)
- 푸시 알림
- 타 대학 확장

---

## 6. v1에 넣지 않는 것 (명시적 제외)

단기 일정에서 범위가 새는 것을 막기 위해 아래를 명시적으로 제외한다.

- 커뮤니티 게시판 (v2)
- 유료 기능·결제
- 소그룹 매칭
- 사진·파일·이모티콘 전송, 읽음 확인, 메시지 삭제·수정
- 유사도 점수·성향 알고리즘 기반 추천
- 시간표·수강 과목 기반 매칭 (데이터 확보 불가, 신원 노출 위험)
- 차단 누적에 따른 자동 정지 (4.5)
- 신고에 대한 이의제기 절차
- 푸시 알림
- 타 대학 확장, 졸업생·교직원 참여

---

## 7. 제약 조건

- **v1의 운영 범위 (2026-08-19 결정)**: v1은 **팀 내 시연 범위**다. 일반 학생을 받지 않고, 팀원 계정으로 전체 흐름을 보여주는 것까지가 목표다. 따라서 이용약관·개인정보처리방침 작성과 대량 메일 발송 수단(커스텀 SMTP)은 v1 범위 밖이다. 아래 개인정보 항목은 **실제 학생을 받기로 결정하는 시점에 갖춰야 할 것**으로 읽는다
- **Supabase 활용 방식 (2026-08-19 결정)**: **실제 Supabase 무료 플랜 프로젝트 하나를 팀이 공유해서 쓴다.** 데이터베이스·인증·RLS·RPC는 지금 설계 그대로 진짜로 동작시킨다 — 화면 흉내나 가짜 데이터가 아니다. 다만 **프로덕션 강화는 하지 않는다**: 유료 플랜 업그레이드, 별도 스테이징/운영 환경 분리, 정기 백업 정책, 커스텀 도메인은 v1 범위 밖이다. 무료 플랜의 제약(용량·정지 정책 등)을 감수한다
- **인증 방식 (2026-08-19 결정)**: v1은 **이메일 주소 입력만으로 로그인**한다. 인증코드도 비밀번호도 요구하지 않는다. 도메인 검사(`@kyonggi.ac.kr`)는 DB 트리거로 유지되므로 "학교 사람만 들어온다"는 4.1의 취지는 지켜지지만, **이메일 주소를 아는 사람은 그 계정으로 들어올 수 있다.** 팀 내 시연 범위에서만 허용되는 지름길이며, 실제 학생을 받기로 하는 순간 이메일 인증코드로 되돌린다. 로그인 상태는 세션으로 유지한다 (9장 15번 해결)
- **일정**: 공모전·해커톤 단기 출품. 정확한 기간 `[확인 필요]`
- **팀**: 인원과 역할 분담 `[확인 필요]`. 단, 신고 검토를 맡을 관리자 1명은 팀 안에서 지정한다
- **이번 산출물**: 본 기획서까지. 구현 형태(네이티브 앱 / 웹 앱 / 단일 HTML), 기술 스택, 서버·DB, 이메일 발송 수단은 모두 `[확인 필요]`
- **서버가 필요한 기능**: 이메일 발송, 검색, 채팅, 신고 처리는 서버와 DB 없이는 실제로 동작하지 않는다. 데모만 만들 경우 어디까지가 실제 동작이고 어디부터가 흉내인지 화면에 명시한다
- **학교 협조**: 학사 시스템·포털 연동은 대학의 공식 승인과 API가 필요해 전제하지 않는다
- **개인정보**: 수집 항목을 이메일·성별·나이·학과·닉네임·관심사 태그·한 줄 소개·(선택)사진으로 한정한다. 보관 기간·탈퇴 시 삭제 정책 `[확인 필요]`

---

## 8. 성공 기준

**주지표: 대화가 이어진 관계 수**

- 정의 — 친구 신청이 수락된 뒤 **양쪽이 각각 3턴 이상** 메시지를 주고받은 관계의 수
- 단순 수락 건수를 쓰지 않는 이유: 예의상 수락하고 대화 없이 끝나는 경우가 성공으로 잡힌다. 양방향 3턴은 "실제로 대화가 시작됐다"에 가장 가까운 측정 가능한 신호다
- 목표 수치와 측정 기간: `[확인 필요]`

**보조 지표**

- 친구 신청 수락률 — 검색과 필터가 적절한 사람을 보여주고 있는지를 드러낸다
- 대면 모드(밸런스게임) 실행 횟수 — 앱이 볼 수 있는 유일한 "실제로 만났다"의 흔적. 부정확하지만 오프라인 전환을 가늠하는 유일한 단서다
- 가입자 수, 재방문율

**성공 기준으로 쓰지 않는 것**: "실제로 친구가 됐는지"는 앱 밖의 일이라 측정할 수 없다. 측정할 수 없는 것을 목표로 삼으면 지표를 조작하게 된다.

---

## 9. 핵심 사용자 흐름

```
[가입]
이메일 입력 → 인증코드 확인 → 프로필 작성(닉네임·나이·학과·성별·관심사 태그·한 줄 소개, 사진은 선택)
   → 가입 완료

[사람 찾기]
필터 설정(성별·나이대·학과·관심사 태그) → 검색 결과 목록 → 프로필 열람
   → 인사말 한 줄과 함께 친구 신청 (하루 5건 상한)
   → 상대 수락 → 1:1 대화방 생성 → 메시지 주고받기
   → 거절·무응답 → 아무 일도 일어나지 않음

[만남]
대화방에서 "만났어요" → 대면 모드 → 밸런스게임 질문 카드 넘기기 (답 저장 없음)

[문제 발생]
차단 → 양방향 완전 숨김 (제재 아님)
신고 → 상호 차단 + 관리자 검토 대기 → 관리자 판단(기각/경고/정지)
```

---

## 10. 미해결 항목 `[확인 필요]`

**제품 결정**

1. 앱 이름
2. 구현 형태(네이티브/웹/단일 HTML)와 기술 스택
3. 관심사 태그 최종 목록과 개수 상한
4. 하루 친구 신청 상한 수치 (제안값: 5건)
5. 검색 결과 정렬 기준 (제안: 최근 접속순)
6. 거절 후 재신청 허용 여부와 대기 기간 (제안: 30일)
7. 로그인 유지 방식 (비밀번호를 두는지, 매번 이메일 인증인지)
8. 밸런스게임 질문 목록 (제안: 30~50개)
9. 채팅 실시간 전송 방식, 대화 보관 기간

**운영·정책**

10. 신고 자동 검토 대기 임계치 (제안값: 서로 다른 사용자 3명)
11. 신고 사유 카테고리 최종 목록
12. 관리자 담당자 지정
13. 정지 기간 기본값과 조정 범위 (제안: 기본 30일)
14. 부적절한 프로필 사진·한 줄 소개 검수 방법
15. 개인정보 보관 기간, 탈퇴 시 데이터 처리
16. 재학 상태 재인증 주기 도입 여부

**목표·자원**

17. 주지표 목표 수치와 측정 기간
18. 매칭이 돌아가기 위한 최소 사용자 규모
19. 커뮤니티(v2) 개설 기준 사용자 수
20. 공모전 기간, 팀 인원과 역할 분담

---

## 부록 A — 확정된 결정 15건

| # | 항목 | 결정 |
|---|---|---|
| 1 | 이번 산출물 | 기획서(SPEC.md)까지 |
| 2 | 문서 용도 | 공모전·해커톤 제출 |
| 3 | 목적 | 목적 불문 "함께할 사람" 찾기 (연애 아님) |
| 4 | 사용자 경계 | 경기대 소속 전체 (학부·대학원·휴학생), 교직원·졸업생·타 대학 제외 |
| 5 | 연령 정책 | 만 19~30세는 안내 문구, 자격 판정은 이메일 인증이 담당 |
| 6 | 인증 방식 | 학교 이메일 6자리 인증코드 |
| 7 | 발견 방식 | 조건 검색 목록 + 친구 신청 (추천·스와이프 없음) |
| 8 | 매칭 단위 | 1:1 (소그룹은 나중 과제) |
| 9 | 채팅 조건 | 신청 수락 후에만 대화 가능, 인사말 한 줄은 필수 |
| 10 | 프로필 중심 | 관심사 태그, 사진은 선택 |
| 11 | 학과 공개 | 정확한 학과 공개, 같은 학과도 검색 허용 |
| 12 | 성별 필터 | 편안함을 위한 선택지, 기본값 "상관없음" |
| 13 | 안전장치 | 차단은 제재와 무관 / 신고 누적 → 관리자 검토 후 정지 |
| 14 | 차별 기능 | 대면 밸런스게임 (답 저장 없음) |
| 15 | 주지표 | 대화가 이어진 관계 수 (양방향 3턴 이상) |

## 부록 B — 선행 소개팅 앱 기획에서 바뀐 것

| 항목 | 소개팅 앱(경기랑) | 이 앱 | 바뀐 이유 |
|---|---|---|---|
| 목적 | 연애 | 목적 불문 친구 | 연애 목적은 진입 부담이 크고, 학교 안에서 실제로 더 흔한 수요는 "같이 뭔가 할 사람"이다 |
| 사용자 | 학부 재학생만 | 학부·대학원·휴학생 | 풀을 넓히고, 교류 경로가 없는 집단을 포함 |
| 발견 | 하루 여러 명 추천 + 하트/패스 | 조건 검색 + 친구 신청 | 목적이 다양하면 시스템이 정해주는 추천보다 사용자가 조건을 거는 편이 맞다. 스와이프는 외모 중심 판단을 유도한다 |
| 프로필 | 사진 공개, 학과는 계열만 | 태그 중심, 사진 선택, 학과 공개 | 친구 찾기에서는 같은 학과·같은 취미를 찾는 수요가 있고, 외모 필터링은 목적에 맞지 않는다 |
| 성별 | "만나고 싶은 성별" 지정 | 편안함 옵션, 기본 "상관없음" | 연애 신호가 아님을 구조로 드러낸다 |
| 안전 | 신고 시 영구 상호 제외, 자동 정지 없음 | 차단/신고 분리 + 신고 누적 시 관리자 검토 | 익명성이 없는 설계라 안전장치를 더 두껍게 해야 한다 |
| 그 외 | — | 대면 밸런스게임 추가 | 오프라인 만남까지 이어지게 하는 것이 이 앱의 차별점 |
